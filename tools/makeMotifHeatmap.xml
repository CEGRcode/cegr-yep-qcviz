<tool id="makeMotifHeatmap" name="Create Motif Heatmap" version="1.0.0">
    <description> from tag pileup heatmapfrequency. </description>
    <macros></macros>
    <requirements>
        <requirement type="package" version="2.7.14">python</requirement>
        <requirement type="package" version="0.22.0">pandas</requirement>
    </requirements>
    <command>
    <![CDATA[
      python $__tool_directory__/makeMotifHeatmap.py
      #for $key in $input_collect.keys()#
        $key
        $input_collect[$key]
      #end for#

    ]]>
    </command>

    <inputs>
        <param name="input_collect" type="data_collection" label="Concatenate Dataset" collection_type="list" />
    </inputs>

    <outputs>
         <data name="sense" format="tabular" from_work_dir="senseData.tabular"/>
         <data name="antisense" format="tabular" from_work_dir="antisenseData.tabular"/>
         <data name="totalTagRankOrder" format="txt" from_work_dir="totalTagOrder.txt"/>
    </outputs>

    <tests>
    </tests>

    <help>
<![CDATA[
**What it does**
    Accepts a collection and returns two separate CDT tabular files, sense and antisense. These can be used to make motif specific heatmaps.
]]>
    </help>

</tool>
